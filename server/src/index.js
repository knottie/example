'use strict';

/* istanbul ignore next */
var server = require('./server');
/* istanbul ignore next */
var routes = require('./routes');

/* istanbul ignore next */
server.listen(3001);
console.log('Server listening.');
